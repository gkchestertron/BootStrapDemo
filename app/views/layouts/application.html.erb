<!DOCTYPE html>
<html>
<head>
  <title>NewAuthDemo</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body data-spy="scroll" data-target=".navbar-example">
  <nav class="navbar navbar-default">
    <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav-list-navbar-collapse-1">
      Menu
      </button>
      </div>
    <div class="collapse navbar-collapse" id="nav-list-navbar-collapse-1">
    <ul class="nav navbar-nav" >
      <li><%= link_to "Home", root_url %> </li>
      <li><%= link_to "Contact", static_pages_contact_url %></li>
      <li><%= link_to "About", static_pages_about_url %></li>
      <li><%= link_to "Help", static_pages_help_url %></li>

<% if !current_user.nil? %>

    <li>Logged in as: <%= current_user.username %></li>
    <li><%= button_to "Logout", session_url, :method => :delete %></li>

<% end %>
 </ul>
</div>

</nav>
<div class="container">
<%= yield %>
</div>
</body>
</html>
